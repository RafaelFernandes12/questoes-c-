#include <bits/stdc++.h>
using namespace std;

int main(){

    int n, m, k; cin >> n, m, k;
    int u, v, c, sum = 0;
    int matriz[3][m];
    for(int i = 0; i < m; i++){
        for(int j = 0; j < 2; j++){
            cin >> matriz[i][j];
        }   
    }

    return 0;
}